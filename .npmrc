# Expo SDK 57 pins react@19.2.3, but @expo/metro-runtime pulls in react-dom@19.2.8,
# whose peer range (^19.2.8) npm's strict resolver then rejects on every install.
# react-dom is web-only and this app is iOS/Android, so the mismatch is inert.
legacy-peer-deps=true
